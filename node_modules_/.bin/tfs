#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../tfs/bin/tfs" "$@"
  ret=$?
else 
  node  "$basedir/../tfs/bin/tfs" "$@"
  ret=$?
fi
exit $ret
